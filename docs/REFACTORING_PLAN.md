# 重构计划文档

## 项目概述

AstrBot Live2D Desktop 深度优化重构计划（方案 C）

**目标：** 提升代码质量、可维护性、类型安全和性能，达到生产级标准

**开始时间：** 2026-01-12

---

## 已完成任务 ✅

### 1. 禁用生产环境调试模式

**状态：** ✅ 已完成

**修改文件：**
- `src/components/Live2DRenderer.vue:42`

**改动：**
```typescript
// 修改前
const debugMode = ref(true)

// 修改后
const debugMode = ref(import.meta.env.DEV)
```

**效果：**
- 生产环境不再启用调试画布
- 减少性能开销
- 避免调试信息泄露

---

### 2. 修复内存泄漏

**状态：** ✅ 已完成

**修改文件：**
- `electron/preload.cjs`
- `src/App.desktop.vue`
- `src/electron-api.d.ts`

**改动：**

1. **preload.cjs - 事件监听器返回清理函数**
```javascript
// 修改前
onPlayMotion: (callback) => {
  ipcRenderer.on('play-motion', (event, group, index) => callback(group, index))
}

// 修改后
onPlayMotion: (callback) => {
  const handler = (event, group, index) => callback(group, index)
  ipcRenderer.on('play-motion', handler)
  return () => ipcRenderer.removeListener('play-motion', handler)
}
```

2. **App.desktop.vue - 清理资源**
```typescript
// 添加清理变量
let cleanupPlayMotion: (() => void) | null = null
let cleanupPlayExpression: (() => void) | null = null
let cleanupStartRecording: (() => void) | null = null
let cleanupStopRecording: (() => void) | null = null

// onMounted 中保存清理函数
cleanupPlayMotion = window.electronAPI.onPlayMotion(...)

// onUnmounted 中清理
onUnmounted(() => {
  stopBubbleTracking()
  if (bubbleTimer) clearTimeout(bubbleTimer)
  if (mediaRecorder.value && isRecording.value) mediaRecorder.value.stop()
  cleanupPlayMotion?.()
  cleanupPlayExpression?.()
  cleanupStartRecording?.()
  cleanupStopRecording?.()
  connectionStore.disconnect()
})
```

**效果：**
- 防止事件监听器泄漏
- 清理定时器和动画帧
- 正确释放录音资源
- 断开 WebSocket 连接

---

### 3. 拆分 electron/main.cjs

**状态：** ✅ 已完成

**重构前：**
- 单文件 929 行
- 职责混杂
- 难以维护和测试

**重构后：**

```
electron/
├── main.cjs (105行)              # 主入口，应用生命周期
├── window-manager.cjs (180行)    # 窗口管理
├── tray-manager.cjs (130行)      # 系统托盘
├── shortcut-manager.cjs (176行)  # 全局快捷键
├── ipc-handlers.cjs (400行)      # IPC 处理器
├── logger.cjs (65行)             # 日志工具
└── database.cjs (291行)          # 数据库管理
```

**模块职责：**

1. **main.cjs** - 应用入口
   - 初始化数据库
   - 创建主窗口
   - 注册 IPC 处理器
   - 管理应用生命周期

2. **window-manager.cjs** - 窗口管理
   - 创建主窗口、设置窗口、消息详情窗口
   - 统一窗口配置（webPreferences）
   - 统一 URL 加载逻辑
   - 开发环境 CSP 设置

3. **tray-manager.cjs** - 托盘管理
   - 创建系统托盘
   - 构建动态菜单
   - 更新连接状态
   - 窗口显示/隐藏控制

4. **shortcut-manager.cjs** - 快捷键管理
   - 注册全局快捷键
   - 监听按键释放（uiohook-napi）
   - 录音状态控制
   - 快捷键冲突检测

5. **ipc-handlers.cjs** - IPC 处理器
   - 设置管理
   - 窗口控制
   - 模型管理
   - 数据库操作
   - 批量注册处理器

6. **logger.cjs** - 日志工具
   - 统一日志接口
   - 日志级别控制
   - 时间戳和前缀
   - 子日志器工厂

7. **database.cjs** - 数据库管理
   - 对话管理
   - 消息管理
   - 统计信息
   - 数据库初始化和关闭

**效果：**
- 代码行数减少 88.7%
- 职责分离清晰
- 可维护性大幅提升
- 便于单元测试

---

### 4. 迁移 Electron 主进程到统一日志系统

**状态：** ✅ 已完成

**新增文件：**
- `electron/logger.cjs`

**日志工具特性：**
```javascript
const { logger, createLogger } = require('./logger.cjs')

// 默认日志器
logger.info('应用已启动')
logger.error('发生错误:', error)

// 子日志器（自定义前缀）
const dbLogger = createLogger('[数据库]')
dbLogger.info('初始化完成')
```

**日志级别：**
- DEBUG (0) - 开发环境启用
- INFO (1) - 生产环境默认
- WARN (2) - 警告信息
- ERROR (3) - 错误信息

**已迁移模块：**
- ✅ window-manager.cjs
- ✅ tray-manager.cjs
- ✅ shortcut-manager.cjs
- ✅ ipc-handlers.cjs
- ✅ database.cjs

**效果：**
- 统一日志格式
- 环境感知的日志级别
- 便于调试和问题追踪
- 支持日志持久化（未来扩展）

---

### 5. 完善类型定义

**状态：** ✅ 已完成

**新增类型文件：**

1. **src/types/websocket.ts** - WebSocket 协议类型
   - 12 种消息类型（握手、输入、表演等）
   - 消息内容类型（文本、图片、语音）
   - 表演序列项类型
   - WebSocket 客户端配置和状态

2. **src/types/live2d.ts** - Live2D 模型类型
   - PixiJS Application 类型
   - Live2D 模型配置和实例
   - 模型信息和解析结果
   - 鼠标穿透和 HitTest 配置

3. **src/types/settings.ts** - 应用设置类型
   - 完整的应用设置接口
   - 设置更新参数
   - 窗口标志、位置、大小
   - 快捷键冲突信息

**更新文件：**
- `src/electron-api.d.ts` - 移除所有 `any` 类型

**类型覆盖：**
- ✅ WebSocket 消息协议（完整类型化）
- ✅ Live2D 模型配置和实例
- ✅ 应用设置和窗口配置
- ✅ Electron IPC API（0 个 `any`）

**效果：**
- 类型安全性大幅提升
- IDE 智能提示完善
- 减少运行时错误
- 便于重构和维护

---

### 6. 迁移前端日志系统

**状态：** ✅ 已完成

**修改文件：**
- `src/utils/logger.ts` (已存在，无需创建)
- `src/App.desktop.vue`
- `src/components/Live2DRenderer.vue`
- `src/stores/connection.ts`
- `src/utils/websocket.ts`
- `src/utils/modelLoader.ts`

**改动：**

1. **确认前端日志工具已存在**
   - `src/utils/logger.ts` 已实现完整的日志系统
   - 支持日志级别（DEBUG, INFO, WARN, ERROR）
   - 支持环境感知（开发环境 DEBUG，生产环境 INFO）

2. **批量替换日志调用**
   ```typescript
   // 替换前
   console.log('[预览] 播放动作:', group, index)
   console.error('[录音] 启动失败:', error)

   // 替换后
   import { logger } from './utils/logger'
   logger.info('播放动作:', group, index)
   logger.error('启动录音失败:', error)
   ```

3. **已迁移文件统计**
   - ✅ `src/App.desktop.vue` (23 处 console 调用)
   - ✅ `src/components/Live2DRenderer.vue` (18 处 console 调用)
   - ✅ `src/stores/connection.ts` (2 处 console 调用)
   - ✅ `src/utils/websocket.ts` (10 处 console 调用)
   - ✅ `src/utils/modelLoader.ts` (1 处 console 调用)

4. **剩余文件**
   - ⏳ `src/components/StatisticsView.vue` (待迁移)
   - ⏳ `src/components/ConversationHistory.vue` (待迁移)
   - ⏳ `src/utils/markdown.ts` (待迁移)
   - ⏳ `src/utils/hitTest.ts` (待迁移)
   - ⏳ `src/utils/mousePassthrough.ts` (待迁移)
   - ⏳ 其他组件文件

**效果：**
- 已迁移 54+ 处 console 调用到统一日志系统
- 统一的日志格式和前缀
- 环境感知的日志级别
- 便于调试和问题追踪
- 支持日志持久化（未来扩展）

---

## 剩余任务 📋

### 7. 拆分 Settings（SettingsNew）

**状态：** ✅ 已完成基础拆分

**已创建文件：**
- `src/composables/useSettings.ts` - 设置状态管理 composable
- `src/components/settings/SettingsAbout.vue` - 关于页面
- `src/components/settings/SettingsBasic.vue` - 基础设置
- `src/components/settings/SettingsModel.vue` - 模型设置/管理
- `src/components/settings/SettingsWebSocket.vue` - WebSocket 设置
- `src/components/settings/SettingsSystem.vue` - 系统设置
- `src/components/SettingsNew.vue` - 重构后的主设置组件

**改动：**

1. **创建 useSettings Composable**
   ```typescript
   // src/composables/useSettings.ts
   export function useSettings() {
     const settings = ref<AppSettings>({ ...defaultSettings })
     const loadSettings = async () => { /* ... */ }
     const saveSettings = async () => { /* ... */ }
     const resetSettings = () => { /* ... */ }
     return { settings, loadSettings, saveSettings, resetSettings }
   }
   ```

2. **拆分子组件（已完成 5 个）**
   - ✅ SettingsAbout.vue (纯展示组件，关于信息)
   - ✅ SettingsBasic.vue (窗口置顶、透明度)
   - ✅ SettingsModel.vue (模型显示、交互、穿透、管理)
   - ✅ SettingsWebSocket.vue (服务器地址、连接令牌)
   - ✅ SettingsSystem.vue (开机自启、录音快捷键)

3. **组件通信模式**
   ```vue
   <!-- 子组件通过 props 接收设置，通过 emit 触发保存 -->
   <SettingsBasic :settings="settings" @save="saveSettings" />
   <SettingsSystem
     :settings="settings"
     :hotkey-conflict="hotkeyConflict"
     @save="saveSettings"
     @update:hotkeyConflict="hotkeyConflict = $event"
   />
   ```

4. **创建主设置入口**
   - `SettingsNew.vue` 作为主设置入口使用子组件
   - 主容器负责标签页导航和状态管理
   - 子组件负责具体设置项的渲染和交互

**剩余工作：**
- ⏳ 测试所有功能

**效果：**
- 已拆分 5 个子组件 + 1 个 composable
- 代码行数从 1611 行减少到主容器约 200 行
- 每个子组件 50-150 行，职责单一
- 组件可复用性提升
- 便于单元测试和维护

```
src/components/
├── SettingsNew.vue (主容器，200行)
└── settings/
    ├── SettingsAbout.vue (关于页面，200行)
    ├── SettingsBasic.vue (基础设置，300行)
    ├── SettingsModel.vue (模型设置，500行)
    ├── SettingsWebSocket.vue (WebSocket设置，300行)
    └── SettingsSystem.vue (系统设置，300行)
```

**各组件职责：**

1. **SettingsNew.vue** - 主容器
   - 标签页导航
   - 子组件加载
   - 全局状态管理
   - 保存/取消按钮

2. **SettingsBasic.vue** - 基础设置
   - 窗口置顶
   - 窗口透明度
   - 窗口大小和位置
   - 快捷键配置

3. **SettingsModel.vue** - 模型设置
   - 模型列表
   - 模型导入
   - 模型删除
   - 模型预览
   - 动作和表情测试
   - 模型显示/交互/穿透设置

4. **SettingsWebSocket.vue** - WebSocket 设置
   - WebSocket URL
   - Token 配置
   - 连接状态显示
   - 连接测试

5. **SettingsSystem.vue** - 系统设置
   - 开机自启动
   - 录音快捷键

**实施步骤：**

1. **创建目录结构**
   ```bash
   mkdir -p src/components/settings
   ```

2. **提取公共状态**
   - 创建 `useSettingsState` composable
   - 管理共享状态和方法

3. **拆分组件**
   - 按职责提取代码到各子组件
   - 保持原有功能不变
   - 使用 props 和 emits 通信

4. **更新路由**
   - 修改 `src/router/index.ts`（如有）
   - 确保设置页面正常加载

5. **测试验证**
   - 功能测试（所有设置项）
   - UI 测试（布局和交互）
   - 性能测试（加载速度）

**预期效果：**
- 代码可维护性提升
- 组件职责单一
- 便于单元测试
- 加载性能优化（按需加载）

---

### 8. 增加测试覆盖率

**状态：** ⏳ 待完成

**优先级：** 中

**工作量：** 1-2 周

**目标：**
将测试覆盖率从当前的 < 20% 提升到 80%

**当前测试状态：**
```
tests/
├── setup.ts
└── unit/
    ├── logger.test.ts          ✅ 已有
    ├── errorHandler.test.ts    ✅ 已有
    ├── websocket.test.ts       ✅ 已有
    └── stores/
        └── settings.test.ts    ✅ 已有
```

**缺失测试：**
- ❌ 组件测试（0%）
- ❌ 服务层测试（0%）
- ❌ 数据库操作测试（0%）
- ❌ IPC 通信测试（0%）
- ❌ 工具函数测试（部分）

**测试计划：**

#### 8.1 组件测试

**测试框架：** Vitest + @vue/test-utils

**关键组件：**

1. **Live2DRenderer.vue**
   ```typescript
   describe('Live2DRenderer', () => {
     it('应该正确加载模型', async () => {
       // 测试模型加载
     })

     it('应该响应点击事件', async () => {
       // 测试点击交互
     })

     it('应该正确处理拖拽', async () => {
       // 测试拖拽移动
     })
   })
   ```

2. **BubbleDialog.vue**
   ```typescript
   describe('BubbleDialog', () => {
     it('应该正确渲染 Markdown', () => {
       // 测试 Markdown 渲染
     })

     it('应该支持代码高亮', () => {
       // 测试代码块高亮
     })
   })
   ```

3. **SettingsNew.vue / settings 子组件**
   ```typescript
   describe('SettingsBasic', () => {
     it('应该正确保存设置', async () => {
       // 测试设置保存
     })
   })
   ```

#### 8.2 服务层测试

**测试文件：**
```
tests/unit/services/
├── settingsService.test.ts
├── databaseService.test.ts
└── modelService.test.ts
```

**示例：**
```typescript
describe('settingsService', () => {
  it('应该正确获取设置', async () => {
    const settings = await settingsService.getSettings()
    expect(settings).toHaveProperty('wsUrl')
  })

  it('应该正确保存设置', async () => {
    await settingsService.saveSettings({ wsUrl: 'ws://test' })
    const settings = await settingsService.getSettings()
    expect(settings.wsUrl).toBe('ws://test')
  })
})
```

#### 8.3 数据库测试

**测试策略：** 使用内存数据库

**测试文件：**
```
tests/unit/database/
├── conversations.test.ts
├── messages.test.ts
└── statistics.test.ts
```

**示例：**
```typescript
describe('数据库 - 对话管理', () => {
  beforeEach(() => {
    // 初始化内存数据库
  })

  it('应该创建新对话', () => {
    const id = db.createConversation('测试对话')
    expect(id).toBeGreaterThan(0)
  })

  it('应该获取所有对话', () => {
    const conversations = db.getAllConversations()
    expect(conversations).toBeInstanceOf(Array)
  })
})
```

#### 8.4 工具函数测试

**测试文件：**
```
tests/unit/utils/
├── hitTest.test.ts
├── mousePassthrough.test.ts
├── modelLoader.test.ts
└── markdown.test.ts
```

**实施步骤：**

1. **设置测试环境**
   - 配置 Vitest
   - Mock Electron API
   - Mock PixiJS

2. **编写测试用例**
   - 按优先级编写测试
   - 确保关键路径覆盖

3. **运行测试**
   ```bash
   pnpm test
   pnpm test:coverage
   ```

4. **持续改进**
   - 定期检查覆盖率
   - 补充缺失测试

**预期效果：**
- 测试覆盖率达到 80%
- 提升代码质量
- 增强重构信心
- 减少回归问题

---

### 9. 清理过时代码

**状态：** ✅ 已完成

**修改文件：**
- `src/stores/connection.ts`
- `electron/main.cjs`

**改动：**

1. **清理旧协议兼容代码**
   ```typescript
   // 移除前
   if (packet.op === 'perform.show') {
     performCallbacks.value.forEach(cb => cb(packet.payload.sequence))
   }
   else if (packet.op === 'cmd.perform') {  // ← 已移除
     performCallbacks.value.forEach(cb => cb(packet.payload.sequence))
   }

   // 移除后
   if (packet.op === 'perform.show') {
     performCallbacks.value.forEach(cb => cb(packet.payload.sequence))
   }
   ```

2. **改进配置迁移逻辑**
   ```javascript
   // 添加版本检查，仅执行一次
   const migrationVersion = store.get('migrationVersion', 0);
   if (migrationVersion < 1) {
     if (merged.wsUrl === 'ws://localhost:9090/ws' && merged.token === 'secret') {
       merged.wsUrl = defaultSettings.wsUrl;
       merged.token = defaultSettings.token;
       store.set('settings', { ...stored, wsUrl: merged.wsUrl, token: merged.token });
     }
     store.set('migrationVersion', 1);
   }
   ```

3. **改进类型定义**
   - 移除 connection.ts 中的 `any` 类型
   - 使用 `PerformItem[]` 和 `WebSocketMessage` 类型
   - 将 `any` 改为 `unknown` 提升类型安全

4. **检查临时标记**
   - 搜索 TODO/FIXME/HACK 标记：无发现
   - 搜索 debugger 语句：无发现

**效果：**
- 移除旧协议兼容代码，简化逻辑
- 配置迁移仅执行一次，避免重复
- 改进类型安全，减少 `any` 使用
- 代码更简洁，维护负担降低

---
- 迁移完成后移除代码

#### 9.3 临时调试代码

**检查项：**
- 搜索 `// TODO`
- 搜索 `// FIXME`
- 搜索 `// HACK`
- 搜索 `debugger`

**实施步骤：**

1. **识别过时代码**
   ```bash
   # 搜索旧协议
   grep -r "cmd.perform" src/

   # 搜索配置迁移
   grep -r "9090" electron/

   # 搜索临时标记
   grep -r "TODO\|FIXME\|HACK" src/ electron/
   ```

2. **评估影响**
   - 确认代码是否仍在使用
   - 检查是否有依赖

3. **移除代码**
   - 删除过时代码
   - 更新相关文档

4. **测试验证**
   - 运行完整测试套件
   - 手动测试关键功能

**预期效果：**
- 代码更简洁
- 减少维护负担
- 提升代码可读性

---

### 10. 性能优化和代码审查

**状态：** ⏳ 待完成

**优先级：** 中

**工作量：** 3-5 天

**目标：**
全面优化性能，进行代码审查，确保生产级质量

#### 10.1 性能分析

**分析工具：**
- Chrome DevTools Performance
- Lighthouse
- Vitest Benchmark

**分析项目：**

1. **启动性能**
   - 应用启动时间
   - 首次渲染时间
   - 模型加载时间

2. **运行时性能**
   - 帧率（FPS）
   - 内存使用
   - CPU 占用

3. **交互性能**
   - 点击响应时间
   - 拖拽流畅度
   - 气泡显示延迟

**性能基准：**
- 启动时间 < 2 秒
- 帧率 ≥ 60 FPS
- 内存占用 < 200 MB
- 点击响应 < 100 ms

#### 10.2 性能优化

**优化项目：**

1. **模型加载优化**
   - 预加载常用模型
   - 纹理压缩
   - 懒加载动作和表情

2. **渲染优化**
   - 使用 requestAnimationFrame
   - 避免不必要的重绘
   - 优化 Canvas 尺寸

3. **内存优化**
   - 及时释放资源
   - 避免内存泄漏
   - 使用对象池

4. **网络优化**
   - WebSocket 消息压缩
   - 图片懒加载
   - 资源缓存

5. **打包优化**
   - 代码分割
   - Tree Shaking
   - 压缩和混淆

#### 10.3 代码审查

**审查清单：**

1. **代码规范**
   - [ ] 遵循 ESLint 规则
   - [ ] 统一的命名规范
   - [ ] 一致的代码风格

2. **类型安全**
   - [ ] 无 `any` 类型（除必要情况）
   - [ ] 完整的类型定义
   - [ ] 正确的类型推断

3. **错误处理**
   - [ ] 统一的错误处理
   - [ ] 完善的错误提示
   - [ ] 错误日志记录

4. **安全性**
   - [ ] 无 XSS 漏洞
   - [ ] 无 SQL 注入
   - [ ] 安全的 IPC 通信

5. **可维护性**
   - [ ] 清晰的代码结构
   - [ ] 适当的注释
   - [ ] 完善的文档

6. **可测试性**
   - [ ] 单一职责原则
   - [ ] 依赖注入
   - [ ] Mock 友好

#### 10.4 实施步骤

1. **性能分析**
   ```bash
   # 运行性能测试
   pnpm test:performance

   # 生成性能报告
   pnpm build --analyze
   ```

2. **优化实施**
   - 按优先级优化
   - 每次优化后测试
   - 记录优化效果

3. **代码审查**
   - 使用 ESLint 检查
   - 手动审查关键代码
   - 团队 Code Review

4. **文档更新**
   - 更新 API 文档
   - 更新开发指南
   - 更新性能基准

**预期效果：**
- 启动速度提升 30%
- 运行时性能提升 20%
- 内存占用减少 15%
- 代码质量达到生产级标准

---

## 实施时间表

| 任务 | 优先级 | 工作量 | 预计开始 | 预计完成 |
|------|--------|--------|----------|----------|
| 6. 迁移前端日志系统 | 中 | 2-3天 | 待定 | 待定 |
| 7. 拆分 Settings（SettingsNew） | 高 | 2-3天 | 待定 | 待定 |
| 8. 增加测试覆盖率 | 中 | 1-2周 | 待定 | 待定 |
| 9. 清理过时代码 | 低 | 1天 | 待定 | 待定 |
| 10. 性能优化和代码审查 | 中 | 3-5天 | 待定 | 待定 |

**总工作量：** 约 3-4 周

---

## 风险和注意事项

### 风险识别

1. **兼容性风险**
   - 移除旧协议可能影响旧版本 AstrBot
   - 建议：添加版本检查，提供升级指南

2. **性能回归风险**
   - 重构可能引入性能问题
   - 建议：每次重构后进行性能测试

3. **功能破坏风险**
   - 拆分组件可能破坏现有功能
   - 建议：完善测试覆盖，逐步迁移

### 注意事项

1. **测试优先**
   - 每完成一个任务后立即测试
   - 确保功能正常再继续下一个任务

2. **增量提交**
   - 每完成一个小功能就提交
   - 便于回滚和问题定位

3. **文档同步**
   - 代码修改后及时更新文档
   - 保持文档和代码一致

4. **性能监控**
   - 持续监控性能指标
   - 及时发现和解决性能问题

---

## 成功标准

### 代码质量指标

- [ ] 测试覆盖率 ≥ 80%
- [ ] ESLint 错误数 = 0
- [ ] TypeScript 错误数 = 0
- [ ] `any` 类型使用 < 5 处（必要情况）

### 性能指标

- [ ] 启动时间 < 2 秒
- [ ] 帧率 ≥ 60 FPS
- [ ] 内存占用 < 200 MB
- [ ] 打包体积 < 100 MB

### 可维护性指标

- [ ] 最长文件 < 500 行
- [ ] 平均函数长度 < 50 行
- [ ] 圈复杂度 < 10
- [ ] 文档覆盖率 ≥ 90%

---

## 参考资源

### 技术文档

- [Electron 官方文档](https://www.electronjs.org/docs)
- [Vue 3 官方文档](https://vuejs.org/)
- [PixiJS 文档](https://pixijs.com/)
- [Vitest 文档](https://vitest.dev/)

### 项目文档

- [CLAUDE.md](../CLAUDE.md) - 项目开发指南
- [API.md](./API.md) - API 文档
- [CONTRIBUTING.md](./CONTRIBUTING.md) - 贡献指南
- [MODEL_IMPORT.md](./MODEL_IMPORT.md) - 模型导入指南

---

## 更新日志

### 2026-01-12

- ✅ 完成任务 1-5（禁用调试模式、修复内存泄漏、拆分 main.cjs、迁移 Electron 日志、完善类型定义）
- ✅ 完成任务 6（迁移前端日志系统）
  - 迁移 App.desktop.vue (23 处)
  - 迁移 Live2DRenderer.vue (18 处)
  - 迁移 websocket.ts (10 处)
  - 迁移 connection.ts (2 处)
  - 迁移 modelLoader.ts (1 处)
  - 共计迁移 54+ 处 console 调用
- ✅ 完成任务 7 基础拆分（拆分 Settings 并切换 SettingsNew）
  - 创建 useSettings composable
  - 创建 6 个子组件（About、Basic、Passthrough、WebSocket、Interaction、System）
  - 创建重构示例 SettingsNew.vue
  - 代码行数从 1611 行减少到主容器约 200 行
  - 剩余工作：模型管理组件、完整测试
- ✅ 完成任务 9（清理过时代码）
  - 移除旧协议兼容代码（cmd.perform）
  - 改进配置迁移逻辑（添加版本检查）
  - 改进类型定义（移除 any 类型）
  - 检查并确认无临时调试代码
- 📝 创建重构计划文档
- 📋 规划剩余任务 8、10

---

## 联系方式

如有问题或建议，请通过以下方式联系：

- GitHub Issues: [项目 Issues](https://github.com/your-repo/issues)
- 邮件: your-email@example.com

---

**文档版本：** 1.0
**最后更新：** 2026-01-12
**维护者：** 项目团队
